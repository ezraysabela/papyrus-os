[development]
network = { rpc-url = "http://localhost:8000/rpc", network-passphrase = "Standalone Network ; February 2017" }
accounts = [{ name = "alice" }]
contracts = { papyrus_registry = { client = true } }

[testnet]
network = { rpc-url = "https://soroban-testnet.stellar.org", network-passphrase = "Test SDF Network ; September 2015" }
accounts = [{ name = "alice" }]
contracts = { papyrus_registry = { client = true } }

[mainnet]
network = { rpc-url = "https://mainnet.sorobanrpc.com", network-passphrase = "Public Global Stellar Network ; September 2015" }
contracts = { papyrus_registry = { client = true, id = "REPLACE_WITH_DEPLOYED_CONTRACT_ID" } }
